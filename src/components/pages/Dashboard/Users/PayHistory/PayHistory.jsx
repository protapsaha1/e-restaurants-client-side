import { Helmet } from "react-helmet-async";

const PayHistory = () => {
    return (
        <div>
            <Helmet>
                <title>Payment History || Red Gold Restaurants</title>
            </Helmet>
            <h1>pay history</h1>
        </div>
    );
};

export default PayHistory;