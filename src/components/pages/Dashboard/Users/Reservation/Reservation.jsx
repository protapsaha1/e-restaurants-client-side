import { Helmet } from "react-helmet-async";

const Reservation = () => {
    return (
        <div>
            <Helmet>
                <title>Reservation || Red Gold Restaurants</title>
            </Helmet>
            <h1>reservation</h1>
        </div>
    );
};

export default Reservation;